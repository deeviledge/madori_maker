name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # index.html が読み込むファイルと sw.js の事前キャッシュ一覧が一致しているか確認
      - name: Check service worker precache list
        run: node tools/update-sw-precache.mjs --check

      # デプロイごとに Service Worker のキャッシュ名を変え、古いキャッシュを一掃する
      - name: Stamp version
        run: node tools/stamp-version.mjs "${GITHUB_SHA::12}"

      # ここは Settings → Pages → Source が「GitHub Actions」になっている必要がある。
      # ワークフローのトークンではPages自体を有効化できないため（enablement: true は
      # "Resource not accessible by integration" で失敗する）、初回だけ画面で設定する。
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
